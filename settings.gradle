plugins {
    // Lets the Java toolchain in build.gradle auto-download a matching JDK (Java 21)
    // when the machine doesn't already have one Gradle can detect.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'Chess'
