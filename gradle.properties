# The JDK is selected by the Java toolchain in build.gradle (Java 21), which
# Gradle auto-detects on each machine. Do NOT set org.gradle.java.home to an
# absolute path here -- it only works on the machine that path exists on and
# breaks the build for everyone else.
